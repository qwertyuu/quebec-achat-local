mixin resourceList(resources, region)
    .ui.segment
        .ui.items.divided
            each resource in resources
                a.item(href=resource.lien target="_blank")
                    .content
                        .header #{resource.titre}
                        .description
                            p !{resource.description}
                        .extra
                            .ui.large.label #{resource.type}
                            if resource.livraison === '1'
                                .ui.large.label Livraison à domicile
                            if resource.preparation_commande === '1'
                                .ui.large.label Cueillette sur place
            else
                h3 Aucune ressource n'est disponible pour le moment.
                h4 Vous pouvez en soumettre une vous-même pour "#{region.nom}" #[a(href=propose_service_raw + currentRegion.nom target="_blank") en cliquant ici].
